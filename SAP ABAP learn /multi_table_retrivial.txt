
TYPES : BEGIN OF TY_MARA,
          MATNR TYPE MATNR,
          MTART TYPE MTART,
          MBRSH TYPE MBRSH,
          MATKL TYPE MATKL,
          MEINS TYPE MEINS,
        END OF TY_MARA.

TYPES : BEGIN OF TY_MAKT,
          MATNR	TYPE MATNR,
          SPRAS	TYPE SPRAS,
          MAKTX TYPE MAKTX,
        END OF TY_MAKT.

TYPES : BEGIN OF TY_FINAL,
          MATNR TYPE MATNR,
          MTART TYPE MTART,
          MBRSH TYPE MBRSH,
          MATKL TYPE MATKL,
          MEINS TYPE MEINS,
          SPRAS	TYPE SPRAS,
          MAKTX TYPE MAKTX,
        END OF TY_FINAL.

DATA : LT_MARA TYPE TABLE OF TY_MARA,
       LS_MARA TYPE TY_MARA,
       LT_MAKT TYPE TABLE OF TY_MAKT,
       LS_MAKT TYPE TY_MAKT.
DATA : LT_FINAL TYPE TABLE OF TY_FINAL,
       LS_FINAL TYPE TY_FINAL.

DATA : LV_MATNR TYPE MATNR.
SELECT-OPTIONS : S_MATNR FOR LV_MATNR.


SELECT MATNR MBRSH MTART MATKL MEINS
  FROM MARA
  INTO TABLE LT_MARA
  WHERE MATNR IN S_MATNR.

IF LT_MARA IS NOT INITIAL.  " is not empty
  SELECT MATNR SPRAS MAKTX
    FROM MAKT
    INTO TABLE LT_MAKT
    FOR ALL ENTRIES IN LT_MARA
    WHERE MATNR = LT_MARA-MATNR.  "Whenever you are going for all entries only write IT.
ENDIF.

*LOOP AT LT_MARA INTO LS_MARA.
*  LOOP AT LT_MAKT INTO LS_MAKT WHERE MATNR = LS_MARA-MATNR.
*
*  ENDLOOP.
*ENDLOOP.

LOOP AT LT_MAKT INTO LS_MAKT.  " item table
  LS_FINAL-SPRAS = LS_MAKT-SPRAS.
  LS_FINAL-MAKTX = LS_MAKT-MAKTX.

  READ TABLE LT_MARA INTO LS_MARA WITH KEY MATNR = LS_MAKT-MATNR.
  IF SY-SUBRC EQ 0. "System variable for return code , if the above operation
                    " is correct it will return 0 otherwwise non zero { 1, 4, 8 etc }
    LS_FINAL-MATNR = LS_MARA-MATNR.
    LS_FINAL-MBRSH = LS_MARA-MBRSH.
    LS_FINAL-MTART = LS_MARA-MTART.
    LS_FINAL-MATKL = LS_MARA-MATKL.
    LS_FINAL-MEINS = LS_MARA-MEINS.
  ENDIF.

  APPEND LS_FINAL TO LT_FINAL.
  CLEAR LS_FINAL.
ENDLOOP.


LOOP AT LT_FINAL INTO LS_FINAL.
  WRITE :/ LS_FINAL-MATNR, LS_FINAL-MBRSH, LS_FINAl-MTART, LS_FINAL-MATKL,
           LS_FINAL-MEINS, LS_FINAL-SPRAS, LS_FINAL-MAKTX.

ENDLOOP.